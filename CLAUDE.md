# CLAUDE.md

## Agent skills

### Issue tracker

Issues tracked as GitHub Issues (uses `gh` CLI). See `docs/agents/issue-tracker.md`.

### Domain docs

Single-context layout: `CONTEXT.md` + `docs/adr/` at repo root. See `docs/agents/domain.md`.
